/**
 * The GitHub label name that triggers the bot to resolve an issue.
 * When this label is added to an issue, the bot self-assigns and
 * begins the resolve-issue workflow.
 */
export const TRIGGER_LABEL = "jared"
