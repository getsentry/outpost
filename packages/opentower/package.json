{
  "name": "opentower",
  "version": "0.4.0",
  "type": "module",
  "license": "MIT",
  "description": "OpenCode plugin: receive GitHub App webhooks and dispatch them to OpenCode agent sessions",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "bin": {
    "opentower": "./src/bin.ts"
  },
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "bun": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "bun run build:dashboard && bun run build:copy",
    "build:dashboard": "bun run --cwd ../dashboard build",
    "build:copy": "rm -rf ./public && cp -r ../dashboard/dist ./public",
    "prepublishOnly": "bun run build"
  },
  "dependencies": {
    "@opencode-ai/sdk": "^1.14.40",
    "@sentry/bun": "^9.0.0",
    "croner": "^10.0.1",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.0.0"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": ">=1.1.1"
  },
  "peerDependenciesMeta": {
    "@opencode-ai/plugin": {
      "optional": true
    }
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^1.14.30",
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.10",
    "typescript": "^5.6.0"
  },
  "files": [
    "src/",
    "public/",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "bun": ">=1.2.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/getsentry/outpost.git",
    "directory": "packages/opentower"
  },
  "publishConfig": {
    "access": "public"
  },
  "keywords": [
    "opencode",
    "plugin",
    "github",
    "webhook",
    "agent",
    "automation"
  ],
  "author": "Aditya Mathur"
}
